name: Sync Issues with Linked PRs and User Project

on:
  schedule:
    - cron: '0 */1 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  repository-projects: write
  pull-requests: read

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Sync Issues, PR State, and Project Status
        uses: actions/github-script@v7
        with:
          script: |

            const ISSUE_OWNER = context.repo.owner;
            const ISSUE_REPO = context.repo.repo;
            const PROJECT_NUMBER = 3;

            console.log(`Repository: ${ISSUE_OWNER}/${ISSUE_REPO}`);

            // ✅ Resolve USER Project
            const projectData = await github.graphql(`
              query($login: String!, $number: Int!) {
                user(login: $login) {
                  projectV2(number: $number) {
                    id
                    fields(first: 50) {
                      nodes {
                        ... on ProjectV2SingleSelectField {
                          id
                          name
                          options {
                            id
                            name
                          }
                        }
                      }
                    }
                  }
                }
              }
            `, {
              login: ISSUE_OWNER,
              number: PROJECT_NUMBER
            });

            const project = projectData.user.projectV2;

            if (!project) {
              console.log("User project not found.");
              return;
            }

            const PROJECT_ID = project.id;

            // ✅ SAFE Status field detection
            const statusField = project.fields.nodes
              .filter(f => f && f.name)
              .find(f => f.name.trim().toLowerCase() === "status");

            if (!statusField) {
              console.log("Status field not found.");
              return;
            }

            const STATUS_FIELD_ID = statusField.id;

            const IN_PROGRESS_OPTION_ID =
              statusField.options?.find(o =>
                o.name?.toLowerCase().includes("progress")
              )?.id;

            const IN_REVIEW_OPTION_ID =
              statusField.options?.find(o =>
                o.name?.toLowerCase().includes("review")
              )?.id;

            if (!IN_PROGRESS_OPTION_ID || !IN_REVIEW_OPTION_ID) {
              console.log("Available Status options:");
              statusField.options?.forEach(o => console.log(`- ${o.name}`));
              return;
            }

            console.log("✅ User Project resolved successfully.");

            // ✅ Get open issues
            const issues = await github.paginate(
              github.rest.issues.listForRepo,
              {
                owner: ISSUE_OWNER,
                repo: ISSUE_REPO,
                state: "open",
              }
            );

            console.log(`Found ${issues.length} open issues`);

            for (const issue of issues) {

              console.log(`\nChecking issue #${issue.number}`);

              const timeline = await github.paginate(
                github.rest.issues.listEventsForTimeline,
                {
                  owner: ISSUE_OWNER,
                  repo: ISSUE_REPO,
                  issue_number: issue.number,
                  mediaType: { previews: ["mockingbird"] }
                }
              );

              for (const event of timeline) {

                if (event.event === "cross-referenced" && event.source?.issue?.pull_request) {

                  const pr = event.source.issue;
                  const prOwner = pr.repository.owner.login;
                  const prRepo = pr.repository.name;
                  const prNumber = pr.number;

                  console.log(`Linked PR: ${prOwner}/${prRepo}#${prNumber}`);

                  let prData;

                  try {
                    const result = await github.request(
                      "GET /repos/{owner}/{repo}/pulls/{pull_number}",
                      {
                        owner: prOwner,
                        repo: prRepo,
                        pull_number: prNumber,
                      }
                    );
                    prData = result.data;
                  } catch {
                    console.log("Cannot access PR.");
                    continue;
                  }

                  console.log(`PR state: ${prData.state}, draft: ${prData.draft}, merged: ${prData.merged}`);

                  if (prData.merged) {
                    await github.rest.issues.update({
                      owner: ISSUE_OWNER,
                      repo: ISSUE_REPO,
                      issue_number: issue.number,
                      state: "closed",
                    });
                    console.log(`✅ Closed issue #${issue.number}`);
                    break;
                  }

                  const targetOptionId =
                    prData.draft
                      ? IN_PROGRESS_OPTION_ID
                      : IN_REVIEW_OPTION_ID;

                  // ✅ Query project items
                  const projectItemsData = await github.graphql(`
                    query($projectId: ID!) {
                      node(id: $projectId) {
                        ... on ProjectV2 {
                          items(first: 100) {
                            nodes {
                              id
                              content {
                                ... on Issue {
                                  id
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  `, { projectId: PROJECT_ID });

                  const issueNodeId = issue.node_id;

                  const projectItem =
                    projectItemsData.node.items.nodes.find(
                      item => item.content?.id === issueNodeId
                    );

                  if (!projectItem) {
                    console.log("Issue not in user project.");
                    continue;
                  }

                  await github.graphql(`
                    mutation($projectId:ID!, $itemId:ID!, $fieldId:ID!, $optionId:String!) {
                      updateProjectV2ItemFieldValue(
                        input:{
                          projectId:$projectId
                          itemId:$itemId
                          fieldId:$fieldId
                          value:{ singleSelectOptionId:$optionId }
                        }
                      ) {
                        projectV2Item { id }
                      }
                    }
                  `, {
                    projectId: PROJECT_ID,
                    itemId: projectItem.id,
                    fieldId: STATUS_FIELD_ID,
                    optionId: targetOptionId
                  });

                  console.log(`✅ Updated project status for issue #${issue.number}`);
                }
              }
            }
